fix: Apply systematic fixes to ViaKoopman compilation errors

This commit addresses 2 compilation errors in ViaKoopman.lean using patterns
from the lean4-theorem-proving skill (measure-theory.md and compilation-errors.md
references).

## Fixes Applied

1. **Line 606: ENNReal/nnnorm coercion**
   - Changed from failing `simp only` to direct rewrite
   - Used `rw [← Real.enorm_eq_ofReal_abs]; rfl`
   - Converts `↑‖f x‖₊ = ENNReal.ofReal |f x|` correctly

2. **Lines 4180-4184: Tendsto.const_mul normalization**
   - Added explicit `simp only [mul_zero]` step
   - Normalizes `2 * Cg * 0 = 0` before using in Tendsto proof
   - Fixes type mismatch in limit expression

3. **Lines 3964-3965: Reverted failing Lp coercion fix to sorry**
   - The coercion equality for birkhoffAverage needs deeper Lp API work
   - Changed back to documented `sorry` with TODO comment
   - This was already a known issue before the fixes

## Remaining Pre-Existing Issues

- **Line 704: BorelSpace metavariable** - Pre-existing `sorry` for instance pollution
  in condExp measurability. Requires specialized measure theory fix.

- **Lines 3965, 3983: Lp norm conversion** - Pre-existing `sorry` blocks for L¹-L²
  Hölder inequality. These are documented TODOs requiring mathlib Lp API refactoring.

- **Line 4549: StandardBorelSpace metavariable** - Pre-existing `sorry` for
  definitional equality proof. Requires more investigation.

## Summary

- **New compilation errors fixed**: 2
- **Pre-existing sorries preserved**: 4
- **Net result**: Reduced active compilation errors while preserving documented TODOs

## Key Patterns Used

- `Real.enorm_eq_ofReal_abs` - ENNReal conversion lemma
- `simp only [mul_zero]` - Explicit arithmetic normalization before Tendsto

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
