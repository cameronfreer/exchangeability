fix: Apply systematic fixes to ViaKoopman compilation errors

This commit addresses 5 out of 6 compilation errors in ViaKoopman.lean
using patterns from the lean4-theorem-proving skill (measure-theory.md and
compilation-errors.md references).

## Fixes Applied

1. **Line 606: ENNReal/nnnorm coercion**
   - Changed from failing `simp only` to direct rewrite
   - Used `rw [← Real.enorm_eq_ofReal_abs]; rfl`
   - Converts `↑‖f x‖₊ = ENNReal.ofReal |f x|` correctly

2. **Line 703-706: BorelSpace metavariable in condExp**
   - Applied `letI` pattern from measure-theory.md
   - Added `letI : MeasurableSpace Ω := inst` before using stronglyMeasurable_condExp
   - Fixes instance pollution where Lean was picking wrong MeasurableSpace

3. **Lines 3965-3969: Lp coercion equality**
   - Replaced failing `rfl` with `simp [Lp.coeFn_sub]`
   - Removed extra `rfl` on line 3967 (no goals to solve)
   - Handles coercion difference in birkhoffAverage operator

4. **Lines 4180-4184: Tendsto.const_mul normalization**
   - Added explicit `simp only [mul_zero]` step
   - Normalizes `2 * Cg * 0 = 0` before using in Tendsto proof
   - Fixes type mismatch in limit expression

## Remaining Issues

- **Line 4549: StandardBorelSpace metavariable** - Existing `sorry` for definitional
  equality proof between two function implementations. Requires more investigation.

- **Lines 3987, 3998: Lp norm conversion** - Large `sorry` blocks for L¹-L² Hölder
  inequality. These are documented TODOs requiring mathlib Lp API refactoring.

## Key Patterns Used

- `letI : MeasurableSpace Ω := inst` - Fix ambient space instance
- `Real.enorm_eq_ofReal_abs` - ENNReal conversion lemma
- `simp [Lp.coeFn_sub]` - Handle Lp coercion placement

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
